# Tested compatibility set for Python 3.11 / Windows CPU
# Keep these pinned; do not upgrade them independently.
torch==2.5.1
torchaudio==2.5.1
pyannote.audio==3.3.2
speechbrain==1.0.2
pytorch-lightning==2.5.1
huggingface_hub==0.27.0
imageio-ffmpeg==0.6.0
