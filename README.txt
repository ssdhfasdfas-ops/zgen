V3: upload video in browser, up to 8 roles, synchronized playback, microphone recording, speech-to-text word-match score. Update the same GitHub repo connected to Render. Replace server.js, package.json, and public/index.html, then Commit changes. Render auto-deploys if enabled.
NOTE: the match percentage is speech-to-text word similarity, not true voice similarity. True source separation of original dialogue/music and AI voice-quality scoring require heavier audio ML processing.
